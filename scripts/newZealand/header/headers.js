export const headersAsHTML = (headersObj) => {
    return `
    <div class="img-header">
    <img src="${headersObj.countryImage}">
    </div>
    <h1 class="country-name">${headersObj.countryName}</h1> 
    `
}





